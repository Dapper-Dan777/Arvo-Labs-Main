# Supabase Configuration
# Kopiere diese Datei zu .env und füge deine Supabase-Credentials ein

VITE_SUPABASE_URL=https://dein-projekt.supabase.co
VITE_SUPABASE_ANON_KEY=dein-anon-key-hier

# Hinweis: 
# - Den ANON/PUBLIC Key findest du unter: Supabase Dashboard → Settings → API → Project API keys → anon/public
# - Der Secret Key wird nur auf dem Server verwendet und gehört NICHT in die .env Datei für Vite!
